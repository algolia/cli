# Local / build defaults (passed through `task build` via -X ldflags — see AGENTS.md)
ALGOLIA_DASHBOARD_URL=
ALGOLIA_API_URL=
ALGOLIA_SEARCH_HOSTS=
ALGOLIA_OAUTH_CLIENT_ID=
ALGOLIA_OAUTH_SCOPE=
# Agent Studio backing host (staging EU default for internal tooling; omit for prod cluster-proxy resolution)
ALGOLIA_AGENT_STUDIO_URL=
